# Database - Turso (SQLite Edge)
# Get your credentials at https://turso.tech (free tier)
TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# Admin Authentication
ADMIN_PASSWORD=your-secure-password
ADMIN_SECRET=generate-a-random-32-character-string

# Sanity CMS
# Get your credentials at https://sanity.io (free tier)
PUBLIC_SANITY_PROJECT_ID=your-project-id
PUBLIC_SANITY_DATASET=production
PUBLIC_SANITY_API_VERSION=2024-01-01

# =============================================================================
# IMAGE STORAGE CONFIGURATION
# =============================================================================
# The system supports two storage providers:
# - 'local' = Files saved to /static/media/ (development + small sites)
# - 'r2' = Cloudflare R2 cloud storage (production + scalable)
#
# Auto-detection: If R2 credentials are set, R2 is used. Otherwise, local.
# You can override by setting STORAGE_PROVIDER explicitly.
# =============================================================================

# Storage Provider: 'local' | 'r2' (optional - auto-detected)
# STORAGE_PROVIDER=local

# Option 1: Local Storage (Default when R2 not configured)
# No configuration needed! Files are saved to /static/media/
# Great for: Development, small sites, self-hosted deployments

# Option 2: Cloudflare R2 Storage (Recommended for production)
# Get your credentials at https://dash.cloudflare.com
# Benefits: 10GB free, FREE egress, global CDN, S3-compatible
R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_BUCKET_NAME=trading-greats-media
R2_PUBLIC_URL=https://pub-xxx.r2.dev

# Site URL (for SEO)
PUBLIC_SITE_URL=https://tradinggreats.com
PUBLIC_SITE_NAME=Trading Greats

# Newsletter / Email (Resend)
# Get your API key at https://resend.com (free tier: 3000 emails/month)
RESEND_API_KEY=re_your_api_key_here
NEWSLETTER_FROM_EMAIL=newsletter@yourdomain.com
NEWSLETTER_FROM_NAME=Trading Greats
